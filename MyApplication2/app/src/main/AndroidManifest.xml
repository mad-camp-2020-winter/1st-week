<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".new_fragment2"></activity>
        <activity android:name=".Address_revise" />
        <activity android:name=".IdealWorldCupResult" />
        <activity android:name=".Tab3Female" />
        <activity android:name=".Tab3Male" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Fragment2Activity" />
        <activity android:name=".Fragment2BookmarksActivity" />
        <activity android:name=".SubActivity" />
    </application>

</manifest>